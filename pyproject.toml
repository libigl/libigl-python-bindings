[tool.cibuildwheel]
build-verbosity = "1"
[build-system]
requires = [
    "numpy>=1.20",
    "scipy",
    "setuptools>=42",
    "wheel",
    "cmake>=3.16",
    "packaging",
    "typing_extensions",
]

build-backend = "setuptools.build_meta"

